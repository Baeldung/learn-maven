<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.baeldung</groupId>
    <artifactId>maven-dependency-management-end</artifactId>
    <version>0.1.0</version>

    <name>maven-dependency-management-end</name>
    <description>Learn Maven - The dependencyManagement Element and BOM - End</description>

    <packaging>jar</packaging>

    <!-- We can use Spring Boot's parent POM instead to inherit all dependency
    management -->
    <!-- <parent>
        <groupId>org.springframework.boot</groupId>
        <version>4.0.7</version>
        <relativePath />
    </parent> -->

    <dependencyManagement>
        <dependencies>
            <!-- JUnit 6.1.0 BOM import: declared before the Boot BOM so it wins;
            a property in the importing POM does not override an imported BOM's
            internal version property -->
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- This is not required, it is included in the Boot BOM-->
            <!-- <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-data-jpa</artifactId>
                <version>${spring.boot.version}</version>
            </dependency> -->
        </dependencies>
    </dependencyManagement>


    <dependencies>
        <!-- web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>

        <!-- data -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <!-- We can override the version from the BOM if needed -->
            <version>${h2.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <!-- additional tasks and configurations for build process -->
    </build>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Prior to JDK 9 and 3.8 Maven versions -->
        <!--
            <maven.compiler.source>21</maven.compiler.source>
            <maven.compiler.target>21</maven.compiler.target>
        -->
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <java.version>25</java.version>
        <spring.boot.version>4.0.7</spring.boot.version>
        <h2.version>2.3.232</h2.version>
        <junit.version>6.1.0</junit.version>
    </properties>
</project>
